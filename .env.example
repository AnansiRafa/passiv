# === Django ===
DJANGO_SETTINGS_MODULE=passiv.settings
DJANGO_DEBUG=True

# === Database ===
DATABASE_URL=postgres://passiv_user:secret@db:5432/passiv

# === Redis / Celery ===
CELERY_BROKER_URL=redis://redis:6379/0

# === GPT / OpenAI ===
OPENAI_API_KEY=sk-...         # Replace with your real OpenAI key

# === Optional: allowed hosts (for prod hardening)
# DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1
